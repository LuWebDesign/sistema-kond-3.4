{
  "name": "sistema-kond-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "set WATCHPACK_POLLING=true && set CHOKIDAR_USEPOLLING=1 && set CHOKIDAR_INTERVAL=200 && next dev --hostname 0.0.0.0 -p 3000",
    "build": "next build",
    "start": "next start -p 3000"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "semver": "^7.7.3"
  }
}
